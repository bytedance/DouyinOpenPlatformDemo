import { IHostEventService, OpenEventParams } from '@douyin/opensdk-common-external'

export class HostEventServiceImpl implements IHostEventService {
  sendEvent(eventName: string, params: OpenEventParams): void {

  }
}